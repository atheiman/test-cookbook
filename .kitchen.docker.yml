---
driver:
  name: docker

platforms:
  - name: centos-6.7
  - name: centos-7.2
  - name: ubuntu-14.04
    run_list:
      - recipe[apt]
  - name: ubuntu-16.04
    run_list:
      - recipe[apt]

suites:
  - name: default
    run_list:
      - recipe[test_cookbook::default]
      # - recipe[fixture_cookbook::default]
    # attributes:
    #   set:
    #     attributes: here
